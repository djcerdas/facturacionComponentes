<persistence xmlns="http://java.sun.com/xml/ns/persistence"
             version="2.0">

    <persistence-unit name="FacturacionPU">

        <!-- Provider -->
       <provider>org.hibernate.ejb.HibernatePersistence</provider>

        <!-- Entidades -->
        <class>entidades.Cliente</class>
        <class>entidades.Compania</class>
        <class>entidades.Individuo</class>

        <class>entidades.Producto</class>
        <class>entidades.Manual</class>
        <class>entidades.Software</class>
        <class>entidades.Hardware</class>

        <class>entidades.Orden</class>
        <class>entidades.ItemOrden</class>

        <!-- Propiedades -->
        <properties>

            <!-- Conexion MySQL -->
            <property name="javax.persistence.jdbc.driver"
                      value="com.mysql.cj.jdbc.Driver"/>

            <property name="javax.persistence.jdbc.url"
                      value="jdbc:mysql://localhost:3306/facturacion_db?useSSL=false&amp;serverTimezone=UTC"/>

            <property name="javax.persistence.jdbc.user" value="root"/>
            <property name="javax.persistence.jdbc.password" value=""/>

            <!-- Hibernate -->
            <property name="hibernate.dialect"
             value="org.hibernate.dialect.MySQLDialect"/>

            <!-- AUTO CREACION DE TABLAS -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>

            <!-- LOG SQL -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>

        </properties>

    </persistence-unit>

</persistence>
